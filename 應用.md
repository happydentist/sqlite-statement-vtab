# [0x09/sqlite-statement-vtab](https://github.com/0x09/sqlite-statement-vtab) 這個專案的核心功能，是讓你可以把「任何 SQL 查詢語句（Statement）」封裝成一個虛擬資料表（Virtual Table）。
簡單來說，它就像是 SQLite 內建的「檢視表（View）」，但比 View 強大好幾倍！因為它支援動態參數化查詢。你可以把它當成一個可以傳入參數的函數表，在查詢的當下才決定篩選條件，這在標準 SQLite View 中是做不到的。
以下為你詳細說明如何應用這個擴充套件：

---
## 應用場景 1：動態參數化檢視表（解決 SQLite View 不能傳參的痛點）
在標準 SQLite 中，View（檢視表）是死板板的。如果你想寫一個複雜的計算，並在查詢時才動態傳入 開始日期 和 結束日期，用 View 會很難寫（必須依賴複雜的 Join 或環境變數）。
透過 statement_vtab，你可以這樣應用：
### 步驟一：註冊一個動態虛擬表
假設你有一張 orders（訂單）表，你想建立一個可以動態查特定日期區間的虛擬表，名稱叫 get_sales：

```sql
-- 註冊虛擬表，將 SQL 語句封裝進去。:1 和 :2 是等待傳入的動態參數
CREATE VIRTUAL TABLE get_sales USING statement(
  'SELECT product_id, SUM(price) FROM orders WHERE order_date BETWEEN :1 AND :2 GROUP BY product_id'
);
```

### 步驟二：在查詢時動態傳入參數
接下來你就可以把這個虛擬表當作普通資料表來用，並在 WHERE 條件中直接帶入參數：
```sql
-- 查詢 2026 年 1 月份的銷售額（此時 '2026-01-01' 會自動填入 :1，'2026-01-31' 填入 :2）
SELECT * FROM get_sales WHERE :1 = '2026-01-01' AND :2 = '2026-01-31';
```
SQLite 的查詢優化器會把這兩個條件直接當作參數送進內部的 SQL 執行，效能極高，且大幅簡化了後端程式碼。

---
## 應用場景 2：封裝超複雜 SQL，簡化後端程式碼
如果你的應用程式（Node.js、Python、Go 等）需要頻繁執行一段長達 50 行、包含大量 JOIN、WINDOW FUNCTION 或 WITH RECURSIVE 的大查詢，把這堆 SQL 塞在後端程式碼裡會變得很醜且難維護。
你可以直接在資料庫初始化時，用這個套件把大查詢包成虛擬表：
```sql
CREATE VIRTUAL TABLE complex_report USING statement(
  'WITH RECURSIVE... (幾十行的複雜 SQL) ... WHERE category = :cat'
);
```
後端程式碼從此只需要呼叫：
```sql
-- 後端只需要寫這行，乾淨俐落
SELECT * FROM complex_report WHERE :cat = 'Electronics';
```

---
## 應用場景 3：動態生成數值數列（類似 PostgreSQL 的 generate_series）
這可以用來動態生成連續的數字或日期（常用於統計報表補空白日期的需求）。
```sql
-- 建立一個數列生成器CREATE VIRTUAL TABLE generate_series USING statement(
  'WITH RECURSIVE seq(n) AS (
     SELECT :start
     UNION ALL
     SELECT n + 1 FROM seq WHERE n < :end
   ) SELECT n FROM seq'
);
-- 動態生成 1 到 10 的數字
SELECT * FROM generate_series WHERE :start = 1 AND :end = 10;
```

---
## 🛠️ 如何在專案中實際啟用它？
你在 GitHub Actions 編編好的 statement_vtab.dll，在程式中載入的方式如下：
### 1. 使用 SQLite 命令列工具 (CLI)
```sql
.load ./statement_vtab.dll-- 接著就可以開始 CREATE VIRTUAL TABLE 了
```
### 2. 在 Node.js (better-sqlite3) 中應用
```javascript
const Database = require('better-sqlite3');const db = new Database('my_project.db');
// 載入你編譯好的擴充套件
db.loadExtension('./statement_vtab.dll');
// 建立動態虛擬表
db.prepare(`
  CREATE VIRTUAL TABLE search_user USING statement(
    'SELECT * FROM users WHERE status = :status AND age > :min_age'
  )
`).run();
// 查詢應用const users = db.prepare('SELECT * FROM search_user WHERE :status = ? AND :min_age = ?').all('active', 18);
console.log(users);
```
### 3. 在 Python (sqlite3) 中應用
```python
import sqlite3
conn = sqlite3.connect('my_project.db')# 啟用擴充套件載入功能
conn.enable_load_extension(True)
conn.load_extension("./statement_vtab.dll")
cursor = conn.cursor()# 建立與查詢應用
cursor.execute("CREATE VIRTUAL TABLE get_user USING statement('SELECT * FROM users WHERE id = :1')")
cursor.execute("SELECT * FROM get_user WHERE :1 = 5")
print(cursor.fetchall())
```
這個套件最厲害的地方在於它突破了 SQLite 檢視表無法傳參的限制，讓你可以把 SQLite 當作更高級的預存程序（Stored Procedure）資料庫來操作。


# 在純 SQL（如 SQLite 命令列工具、DBeaver、Navicat 或任何支援 SQL 的資料庫管理軟體）中，這個擴充套件最經典的應用就是「用 SQL 來產生 SQL」，以及「建立動態報表查詢」。
在純 SQL 的環境下，因為我們沒有後端程式語言（如 Python、Node.js）來幫忙填入變數，我們主要是透過 WHERE 條件的隱式傳參，或結合 JOIN 語法來實現超強大的動態應用。
以下是純 SQL 環境下的三大實戰應用方法：

---

## 應用 1：解決國定假日、業績報表的「動態日期區間」查詢
在純 SQL 中，如果你要拉一段日期區間的複雜業績分析，通常必須每次都修改 WHERE 後面的日期。利用這個套件，你可以把這段邏輯封裝，直接在最外層下條件。
### 1. 建立動態區間報表虛擬表
```sql
CREATE VIRTUAL TABLE monthly_report_vtab USING statement(
  'SELECT 
     p.category AS 分類,
     COUNT(o.id) AS 總訂單數,
     SUM(o.amount) AS 總營業額
   FROM orders o
   JOIN products p ON o.product_id = p.id
   WHERE o.order_date BETWEEN :start_date AND :end_date
   GROUP BY p.category'
);
```
## 2. 純 SQL 查詢應用
當你要查不同月份時，不需要動到裡面的複雜 JOIN，直接在最外層給參數：
```sql
-- 查 2026 年第一季業績
SELECT * FROM monthly_report_vtab WHERE :start_date = '2026-01-01' AND :end_date = '2026-03-31';
-- 查 2026 年端午節連假業績
SELECT * FROM monthly_report_vtab WHERE :start_date = '2026-06-15' AND :end_date = '2026-06-18';
```
---
## 應用 2：神奇的「動態 JOIN」應用（查多筆資料的進階技巧）
這是在純 SQL 中最神奇的用法。如果你有一張 customers（客戶）資料表，你想針對「每一個客戶」動態代入參數去查他個人的專屬虛擬表報告，你可以直接用 JOIN。
### 1. 建立單一客戶的複雜足跡查詢
```sql
CREATE VIRTUAL TABLE user_behavior USING statement(
  'SELECT :user_id AS c_id, action, COUNT(*) as action_count 
   FROM logs 
   WHERE user_id = :user_id 
   GROUP BY action'
);
```
### 2. 用一張表去 JOIN 虛擬表（批量動態傳參）
你可以讓 customers 表的 id 自動倒進虛擬表的 :user_id 參數中：
```sql
SELECT 
  c.name AS 客戶名稱,
  b.action AS 操作行為,
  b.action_count AS 次數FROM customers cJOIN user_behavior b ON b.:user_id = c.id  -- 這裡會自動把 c.id 當作參數傳入虛擬表！WHERE c.vip_level = 'Gold';
```
這在傳統 SQL 中需要寫極度複雜的子查詢（Subquery）或 Window Function，但在這裡就像呼叫函數一樣自然。

---
## 應用 3：利用 CTE 遞迴在純 SQL 中動態生成萬年曆
在純 SQL 做統計時，最常遇到「某天沒有訂單，圖表就會斷掉」的問題。我們需要一張「連續日期表」來做 LEFT JOIN。利用這個套件，你可以用純 SQL 寫一個動態日期生成器：
### 1. 建立動態日期生成器
```sql
CREATE VIRTUAL TABLE date_generator USING statement(
  'WITH RECURSIVE calendar(d) AS (
     SELECT :start_day
     UNION ALL
     SELECT date(d, "+1 day") FROM calendar WHERE d < :end_day
   ) SELECT d FROM calendar'
);
```
### 2. 在純 SQL 中應用：補齊空白日期的營業額
我們想要查 2026 年 7 月份每天的營業額，即使沒人點單的那天也要顯示 $0：
```sql
SELECT 
  g.d AS 日期,
  COALESCE(SUM(o.amount), 0) AS 當日總營業額FROM date_generator gLEFT JOIN orders o ON o.order_date = g.dWHERE g.:start_day = '2026-07-01' AND g.:end_day = '2026-07-31'GROUP BY g.d;
```

這裡為您再提供三個非常實用的純 SQL 進階實戰場景。
這些場景在傳統 SQLite 中通常需要寫出大量重複、難以維護的巢狀子查詢（Subquery），或是根本辦不到。但在載入您編編好的 sqlite-statement-vtab 擴充套件後，全部都能用優雅的純 SQL 輕鬆搞定。

---
## 應用 4：電商系統的「動態多重折扣與階梯定價」計算
在電商或進銷存系統中，產品的最終售價往往取決於「客戶等級」與「購買數量（階梯集體折扣）」。這部分的計算邏輯通常很複雜。
### 1. 用虛擬表封裝動態定價邏輯
我們建立一個名為 calculate_price 的虛擬表，需要傳入三個參數：:prod_id（商品）、:cust_id（客戶等級）、:qty（購買數量）：
```sql
CREATE VIRTUAL TABLE calculate_price USING statement(
  'SELECT 
     p.id AS product_id,
     :qty AS quantity,
     -- 根據客戶等級與數量，動態計算最終折扣金額
     (p.base_price * (CASE WHEN c.level = "VIP" THEN 0.8 ELSE 1.0 END) * 
      CASE WHEN :qty >= 100 THEN 0.9 WHEN :qty >= 10 THEN 0.95 ELSE 1.0 END) AS final_unit_price
   FROM products p
   LEFT JOIN customers c ON c.id = :cust_id
   WHERE p.id = :prod_id'
);
```
### 2. 在純 SQL 中應用：直接與購物車明細進行 JOIN
當您要結帳一張包含多個品項的購物車（cart_items）時，不用在後端算，用純 SQL 就能動態算出整張單的總價：
```sql
SELECT 
  cart.user_id,
  cart.product_id,
  cart.qty,
  price_vtab.final_unit_price,
  (cart.qty * price_vtab.final_unit_price) AS item_total_priceFROM cart_items cartJOIN calculate_price price_vtab 
  ON price_vtab.:prod_id = cart.product_id
 AND price_vtab.:cust_id = cart.user_id
 AND price_vtab.:qty = cart.qty; -- 數量也是動態傳入的參數！
```
---
## 應用 5：無痛實現「動態權限控管與資料列審計（Row-Level Security）」
如果您在做後台管理系統，希望不同的管理員（Admin）登入時，看到的資料範圍不同（例如：A 經理只能看「北部」的客戶，B 專員只能看「自己負責」的客戶）。
### 1. 建立動態權限過濾虛擬表
我們建立一個 secure_customer_list 虛擬表，每次查詢都必須強制傳入當前操作的 :operator_id：
```sql
CREATE VIRTUAL TABLE secure_customer_list USING statement(
  'SELECT c.* 
   FROM customers c
   JOIN admin_users a ON a.id = :operator_id
   WHERE 
     a.role = "SuperAdmin" -- 如果是超級管理員，看全部
     OR (a.role = "Manager" AND c.region = a.managed_region) -- 經理只能看所屬區域
     OR (a.role = "Sales" AND c.sales_owner_id = a.id) -- 業務只能看自己的客戶'
);
```
### 2. 純 SQL 查詢應用
在報表工具中，您只要綁定當前登入的用戶 ID，後續的所有查詢就絕對不會越權洩漏資料：
```sql
-- 行銷部小張（ID: 105，角色是 Sales）要拉客戶清單
SELECT name, phone FROM secure_customer_list WHERE :operator_id = 105;
-- 總經理（ID: 1，角色是 SuperAdmin）要拉同一個清單
SELECT name, phone FROM secure_customer_list WHERE :operator_id = 1;
```
這能確保核心的過濾邏輯（WHERE Role...）永遠被鎖在資料庫內，查詢的人完全不用擔心漏寫過濾條件而造成資安漏洞。

---
## 應用 6：萬用資料庫圖表——「動態區間 X 軸」樞紐分析（Pivot）
在畫商業圖表時，使用者常常會點選不同的時間顆粒度（按小時、按天、按月）來檢視銷售趨勢。在 SQLite 裡要動態切換時間分組非常痛苦。
### 1. 建立動態時間分組虛擬表
這個虛擬表接收 :time_unit（傳入 'hour', 'day', 'month'）與區間參數：
```sql
CREATE VIRTUAL TABLE dynamic_trend USING statement(
  'SELECT 
     CASE 
       WHEN :time_unit = "hour" THEN strftime("%Y-%m-%d %H:00", order_date)
       WHEN :time_unit = "day"  THEN strftime("%Y-%m-%d", order_date)
       WHEN :time_unit = "month" THEN strftime("%Y-%m", order_date)
     END AS time_bucket,
     COUNT(*) AS order_count,
     SUM(amount) AS revenue
   FROM orders
   WHERE order_date BETWEEN :start AND :end
   GROUP BY time_bucket'
);
```
### 2. 純 SQL 查詢應用
您可以根據前端圖表按鈕切換的結果，在純 SQL 中直接拉取完全不同顆粒度的統計圖表數據：
```sql
-- 場景 A：使用者想看「今天」每小時的訂單波動
SELECT time_bucket, order_count FROM dynamic_trend WHERE :time_unit = 'hour' AND :start = '2026-07-30 00:00:00' AND :end = '2026-07-30 23:59:59';
-- 場景 B：使用者切換想看「今年」每個月的營收大趨勢
SELECT time_bucket, revenue FROM dynamic_trend WHERE :time_unit = 'month' AND :start = '2026-01-01' AND :end = '2026-12-31';
```
---
## ⚠️ 純 SQL 環境下的重要使用規則
在純 SQL（如 CLI 或圖形化工具）操作此虛擬表時，請謹記以下兩點：
   1. 必須提供所有參數：你在 CREATE VIRTUAL TABLE 時寫了幾個帶冒號的參數（如 :1, :start），在 SELECT 的 WHERE 條件中就必須全部指定，否則 SQLite 會因為缺少參數而報錯或查不出資料。
   2. 參數名稱要完全一致：虛擬表欄位名稱會自動包含冒號，在下條件時請寫 WHERE :start_date = '...'。

# 只能在 WHERE 傳參嗎
答案是：不一定只能在 WHERE 傳參，但這是最安全且最常見的做法。
在 SQLite 的虛擬資料表機制中，傳遞參數本質上是利用 SQL 的 「查詢優化器（Query Planner）」 來做對應。除了最直覺的 WHERE :param = 'value' 之外，還有另外兩種非常實用的傳參方式：

---
## 1. 利用 FROM 子查詢或 VALUES 語法傳參
如果您不想把參數打散在 WHERE 的各個角落，可以使用 FROM 後面跟著 VALUES 的寫法，把參數當作一個「暫時的對照表」直接灌進去。
### 範例：
假設您有一個計算折扣的虛擬表 :product_type 和 :discount_rate：
```sql
SELECT v.* FROM (VALUES('Electronics', 0.85)) AS p(type, rate)JOIN discount_vtab v 
  ON v.:product_type = p.type 
 AND v.:discount_rate = p.rate;
```
這種寫法的優點是，參數被集中在 VALUES(...) 裡面，看起來非常像在呼叫一個帶有參數的函數。

---
## 2. 利用 JOIN 的 ON 條件傳參（最強大的動態玩法）
這就是前一題提到的方式。參數不一定要寫死一個固定值，它可以綁定到另外一張實體資料表的欄位上。當您進行 JOIN 時，主表的每一列數據在掃描的當下，會動態地把該欄位的值當成參數「射」進虛擬表裡。
### 範例：
我們有一張實體表 departments（部門表，有 dept_id 欄位），想要動態撈出每個部門的員工考績統計：
```sql
SELECT 
  d.dept_name, 
  e.avg_scoreFROM departments dJOIN dept_employee_summary_vtab e 
  ON e.:target_dept = d.dept_id; -- 參數直接綁定在 ON 條件上！
```
在這個操作中，您完全沒有寫 WHERE，但 SQLite 在執行這段 SQL 時，會自動針對每一個部門的 dept_id 去觸發虛擬表內部的查詢。

---
## 為什麼大家最常看到在 WHERE 傳參？（底層原理）
這與 SQLite 的擴充套件底層設計有關。這個專案（sqlite-statement-vtab）在底層實作了 SQLite 的 xBestIndex 函數。
當您寫下：

* WHERE :param = 'XYZ'
* 或 ON v.:param = d.id

SQLite 的優化器看到 「虛擬表的某個特定欄位（即參數欄位）正在進行 EQ（等於）的比較」 時，它就會跟虛擬表說：「嘿！使用者傳進來了這個值，請把它當作篩選條件。」
## ❌ 哪些地方「不能」用來傳參？
因為它本質上還是要過優化器的欄位比較，所以您不能把參數放在以下地方：

* 不能放在 SELECT 欄位中傳參：例如 SELECT :param FROM my_vtab; ❌（這會變成單純輸出變數，無法傳入內部）。
* 不能放在 ORDER BY 或 GROUP BY 後面：例如 SELECT * FROM my_vtab ORDER BY :sort_col; ❌。

## 💡 總結純 SQL 傳參的最佳實踐
如果您只想查單一組合，用 WHERE :p1 = x AND :p2 = y 最直覺；如果您要批量查多組資料，用 JOIN ... ON v.:p1 = main.id 或是 FROM (VALUES(...)) 則是靈活度最高的進階技巧。

# 傳參方式是 在 單純 sqlite 就可用，還是須加上這個擴充功能

答案是：必須加上這個擴充功能（sqlite-statement-vtab）才做得到。單純、內建的 SQLite 是完全不支援這種傳參方式的。
這正是你大費周章在 GitHub Actions 編譯這個 DLL 的真正價值所在！
以下為你對比「單純 SQLite」與「加上擴充功能」在底層與語法上的本質差別：

## 1. 單純的 SQLite：View（檢視表）絕對不支援傳參
在沒有這個擴充功能的情況下，SQLite 內建的 CREATE VIEW 只能封裝死板的查詢。

* 內建 SQLite 辦不到的事：你無法建立一個 View，然後在查詢時才把參數傳進 View 的肚子裡（例如傳入動態日期）。
* 內建 SQLite 的妥協做法：你必須每次都寫出完整的、好幾十行的複雜大 SQL 語句，並在最外層下 WHERE。但如果參數是埋在 SQL 語句內部的子查詢（Subquery）中，內建的 View 就徹底無能為力。

## 2. 加上擴充功能：把「參數」偽裝成「資料表欄位」
這個擴充功能（sqlite-statement-vtab）利用了 SQLite 的虛擬表（Virtual Table）黑魔法。它的原理是：

   1. 當你定義 :start_date 時，這個擴充功能會欺騙 SQLite 核心，讓 SQLite 認為這個虛擬表有兩個欄位：一個叫 data（實際資料），一個叫 :start_date（參數）。
   2. 當你寫下 WHERE :start_date = '2026-07-01' 時，單純的 SQLite 誤以為你只是在對一個叫做 :start_date 的「欄位」進行標準的等於（=）篩選。
   3. 但在底層，這個擴充功能會偷偷把 '2026-07-01' 這個數值攔截下來，並像外掛一樣，把這個值塞進你原本封裝好的長 SQL 語句中，最後把結果吐回給 SQLite。

## 唯一例外：單純 SQLite 類似的進階語法（資料表值函數）
單純、不加外掛的 SQLite 只有在極少數內建的功能中，才看得到一種類似「呼叫函數並傳參」的寫法，叫做 Table-Valued Functions（資料表值函數）。
例如內建的 json_each 函數，可以把 JSON 字串轉成表格：
```sql
-- 這是單純 SQLite 內建的，不需要外掛
SELECT * FROM json_each('["apple", "banana"]');
```
然而，內建的 SQLite 不允許使用者自己用純 SQL 去創造這種函數。如果你想要自己客製化一個「輸入參數、吐出表格」的功能，就必須使用你編譯好的這個虛擬表擴充套件，才能用 WHERE :param = ... 或 ON v.:param = ... 的語法來達成。
既然你的 DLL 已經成功編譯出來了，你想試著把這段「動態傳參」的純 SQL 語法，實際在你的資料庫工具（例如 DBeaver 或命令列）中執行看看嗎？我可以教你如何在純 SQL 工具中下指令載入這個 DLL。

---
## 💡 總結這個擴充功能的核心價值
如果用一句話總結這個擴充套件，它就是「把一段帶有未知變數的 SQL 劇本，變成一個可以重複插拔、與其他資料表互相 JOIN 的資料元件」。這讓 SQLite 擁有了類似大型微軟 SQL Server 或 Oracle 資料庫才有的「表值函數（Table-Valued Functions）」高級功能，非常適合用在複雜報表、動態權限與電商定價計算中！
看完這些實戰場景後，在您目前手邊的專案或資料庫中，是否有哪一段特別冗長或需要頻繁修改變數的 SQL 語句呢？如果您願意，可以把那段 SQL 貼給我，我們一起把它封裝成這個超酷的虛擬表！
