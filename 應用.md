# [0x09/sqlite-statement-vtab](https://github.com/0x09/sqlite-statement-vtab) 這個專案的核心功能，是讓你可以把「任何 SQL 查詢語句（Statement）」封裝成一個虛擬資料表（Virtual Table）。
簡單來說，它就像是 SQLite 內建的「檢視表（View）」，但比 View 強大好幾倍！因為它支援動態參數化查詢。你可以把它當成一個可以傳入參數的函數表，在查詢的當下才決定篩選條件，這在標準 SQLite View 中是做不到的。
以下為你詳細說明如何應用這個擴充套件：

---
## 應用場景 1：動態參數化檢視表（解決 SQLite View 不能傳參的痛點）
在標準 SQLite 中，View（檢視表）是死板板的。如果你想寫一個複雜的計算，並在查詢時才動態傳入 開始日期 和 結束日期，用 View 會很難寫（必須依賴複雜的 Join 或環境變數）。
透過 statement_vtab，你可以這樣應用：
## 步驟一：註冊一個動態虛擬表
假設你有一張 orders（訂單）表，你想建立一個可以動態查特定日期區間的虛擬表，名稱叫 get_sales：

```sql
-- 註冊虛擬表，將 SQL 語句封裝進去。:1 和 :2 是等待傳入的動態參數
CREATE VIRTUAL TABLE get_sales USING statement(
  'SELECT product_id, SUM(price) FROM orders WHERE order_date BETWEEN :1 AND :2 GROUP BY product_id'
);
```

## 步驟二：在查詢時動態傳入參數
接下來你就可以把這個虛擬表當作普通資料表來用，並在 WHERE 條件中直接帶入參數：
```sql
-- 查詢 2026 年 1 月份的銷售額（此時 '2026-01-01' 會自動填入 :1，'2026-01-31' 填入 :2）
SELECT * FROM get_sales WHERE :1 = '2026-01-01' AND :2 = '2026-01-31';
```
SQLite 的查詢優化器會把這兩個條件直接當作參數送進內部的 SQL 執行，效能極高，且大幅簡化了後端程式碼。

---
## 應用場景 2：封裝超複雜 SQL，簡化後端程式碼
如果你的應用程式（Node.js、Python、Go 等）需要頻繁執行一段長達 50 行、包含大量 JOIN、WINDOW FUNCTION 或 WITH RECURSIVE 的大查詢，把這堆 SQL 塞在後端程式碼裡會變得很醜且難維護。
你可以直接在資料庫初始化時，用這個套件把大查詢包成虛擬表：
```sql
CREATE VIRTUAL TABLE complex_report USING statement(
  'WITH RECURSIVE... (幾十行的複雜 SQL) ... WHERE category = :cat'
);
```
後端程式碼從此只需要呼叫：
```sql
-- 後端只需要寫這行，乾淨俐落
SELECT * FROM complex_report WHERE :cat = 'Electronics';
```

---
## 應用場景 3：動態生成數值數列（類似 PostgreSQL 的 generate_series）
這可以用來動態生成連續的數字或日期（常用於統計報表補空白日期的需求）。
```sql
-- 建立一個數列生成器CREATE VIRTUAL TABLE generate_series USING statement(
  'WITH RECURSIVE seq(n) AS (
     SELECT :start
     UNION ALL
     SELECT n + 1 FROM seq WHERE n < :end
   ) SELECT n FROM seq'
);
-- 動態生成 1 到 10 的數字
SELECT * FROM generate_series WHERE :start = 1 AND :end = 10;
```

---
## 🛠️ 如何在專案中實際啟用它？
你在 GitHub Actions 編編好的 statement_vtab.dll，在程式中載入的方式如下：
## 1. 使用 SQLite 命令列工具 (CLI)
```sql
.load ./statement_vtab.dll-- 接著就可以開始 CREATE VIRTUAL TABLE 了
```
## 2. 在 Node.js (better-sqlite3) 中應用
```javascript
const Database = require('better-sqlite3');const db = new Database('my_project.db');
// 載入你編譯好的擴充套件
db.loadExtension('./statement_vtab.dll');
// 建立動態虛擬表
db.prepare(`
  CREATE VIRTUAL TABLE search_user USING statement(
    'SELECT * FROM users WHERE status = :status AND age > :min_age'
  )
`).run();
// 查詢應用const users = db.prepare('SELECT * FROM search_user WHERE :status = ? AND :min_age = ?').all('active', 18);
console.log(users);
```
## 3. 在 Python (sqlite3) 中應用
```python
import sqlite3
conn = sqlite3.connect('my_project.db')# 啟用擴充套件載入功能
conn.enable_load_extension(True)
conn.load_extension("./statement_vtab.dll")
cursor = conn.cursor()# 建立與查詢應用
cursor.execute("CREATE VIRTUAL TABLE get_user USING statement('SELECT * FROM users WHERE id = :1')")
cursor.execute("SELECT * FROM get_user WHERE :1 = 5")
print(cursor.fetchall())
```
這個套件最厲害的地方在於它突破了 SQLite 檢視表無法傳參的限制，讓你可以把 SQLite 當作更高級的預存程序（Stored Procedure）資料庫來操作。
請問你目前打算把這個功能應用在哪一種程式語言（例如 Python、Node.js、C#）的專案中呢？我可以為你提供該語言專屬的完整連接範例。
